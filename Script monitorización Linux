#!/bin/bash

SERVICIOS=("ssh" "apache2" "mysql")

for servicio in "${SERVICIOS[@]}"; do
    if systemctl is-active --quiet "$servicio"; then
        echo " $servicio está en funcionamiento. "
    else
        echo " $servicio está caído."
    fi
done
